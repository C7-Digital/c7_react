shamefully-hoist=true
strict-peer-dependencies=false

# When publishing to GitHub, the workspace protocol will be preserved
# Users installing from GitHub will need to have @c7/ledger available
# or specify it in their own package.json
